# Plantilla de secretos para Streamlit Community Cloud.
#
# LOCAL:  copie a `.streamlit/secrets.toml` (ya ignorado por git).
# NUBE:   pegue este contenido en Settings > Secrets de la app desplegada.
#
# El archivo real `.streamlit/secrets.toml` NUNCA se versiona.

GROQ_API_KEY = "gsk_reemplace_esto_por_su_clave_real"
