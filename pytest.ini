[pytest]
testpaths = tests
markers =
    smoke: ejecuta la app completa con AppTest de Streamlit (lento, ~45 s)

# Iteración rápida durante el desarrollo:  pytest -m "not smoke"
# Suite completa antes de entregar:        pytest
